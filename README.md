# remix-on-blog

Cloudflare Pages へデプロイ:

```bash
npm run deploy
```

microCMS を使う場合は、Cloudflare Pages の環境変数に `MICROCMS_API_KEY` と `MICROCMS_SERVICE_DOMAIN` を設定してください。必要なら `MICROCMS_ENDPOINT` も指定できます。
